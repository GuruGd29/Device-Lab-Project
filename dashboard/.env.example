# Base URL of the cloud control plane (REST + WS host). The WS URL is derived from this
# (http -> ws, https -> wss) in src/lib/config.ts. Defaults to http://localhost:8080.
VITE_API_URL=http://localhost:8080
